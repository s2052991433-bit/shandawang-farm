# 山大王农场项目备忘

## 当前交接状态（2026-09-19）

- 用户明确选定“老树下的拾蛋小院”主视觉，并授权落实动效与整体协调性审查。保留原秋景及同构图春、夏、冬版本，新增同构图雨天、夜间光照素材，各有1672/960宽WebP。统一Canvas2D渲染，加入局部枝叶摆动、鸡群头部微动、山谷薄雾、分层雨雪、檐滴和碎石路雨点。鸡群仅为局部照片网格变形，不是行走、骨骼动画或实拍视频；自然感仍待用户验收。
- 季节按中国时间自动变化；天气由/api/weather经Worker请求MET Norway区域预报，设缓存、超时和失败回退。预览环境真实请求超时，尚未完成线上天气连通验收。默认29.87/121.55仅代表宁波地区；准确农场位置未提供。可配置FARM_WEATHER_LAT、FARM_WEATHER_LON、FARM_WEATHER_LABEL。界面区分手动演示、区域预报与天气暂不可用，不伪造实时天气。
- 首页收拢为农场介绍与入口，鸡蛋年卡提前；商城默认当季货架，农场页保留实际日期的日志，三页共用小院并保留手动天气设置。年卡与礼赠兑换入口分别说明权益，客户仍只输入卡密。没有改动已导入卡券、数据库或管理员凭据。
- 节气与农历改为实际中国时间计算，旧日志不会重标成今天；示例内容只在开发/评审模式出现，生产没有真实日志时显示空状态。手机天气设置为独立可关闭面板，包含焦点约束、Esc关闭、暂停和减少动态支持。
- 统一Canvas2D后无需WebGL分支；云浏览器已检查桌面与390/760宽响应式布局，原生iOS Safari尚未实测。新增日期、局部运动边界与遮挡测试，加上天气和Worker共30项测试。完整离线评审文件由scripts/build-site-review.mjs从vite --mode review构建生成，内嵌图片、保留站内导航，不请求真实天气。正式网站尚未部署，本次保留在PR #3草稿。
- 已删除第一版雾气组件及渲染器，竹林池塘试作也已被用户否决。不得重新发布这两版。
- 本次使用sites-preview和http://terminal.local:4173/成功打开云浏览器预览；旧的localhost/file协议尝试不应继续。已经检查电脑端与390/760宽iframe的实际响应式页面；尚未发布正式站，手机Safari实机未测试。
- 用户已确认100张原卡密导入成功，并完成过一次真实兑换，后台收到记录。
- 腾讯云国内短信控制台显示1100条余量。资质报备等待用户上传营业执照，按用户要求暂停；顾客注册、签名模板审核、Cloudflare直连发信尚未完成。

## 发布与运行架构

以下决定于 2026-08-31 确认：

- GitHub 仓库 `s2052991433-bit/shandawang-farm` 是网站唯一源码来源。
- Cloudflare Workers 与 D1 是网站唯一正式运行环境，不保留 GitHub Pages、ChatGPT Sites 或其他旧站作为并行公网版本。
- `www.shandawangfarm.com` 是正式规范网址；根域名 `shandawangfarm.com` 保留原路径和查询参数，永久重定向到 `www`。
- 标准发布顺序是：先把新版同步到 GitHub，由 Cloudflare 构建和部署；先用临时 Worker 地址完成验收，再切换正式域名；切换后同时验证桌面端、手机端、商品接口、后台入口和根域名重定向。
- 正式域名切换前必须核对并移除旧站 DNS 记录，避免新旧版本并存或流量误入旧托管。

## 实体鸡蛋年卡导入（2026-09-19）

- 已确认本批已有卡号和密码对应鸡蛋年卡，必须保留原有信息，不重新生成。
- 权益：面值798元，2027年1月至12月，每月1箱30枚，共12次360枚；不含鸡肉加赠。
- 新增农场主专用导入入口，支持从Excel粘贴卡号、密码两列或读取两列CSV。每批最多100张，相同卡券重导跳过，任何冲突整批拒绝。导入和审计写入同一数据库事务。
- 客户只输入卡密兑换，无需输入编号。原表NO.001等编号仅供后台识别，导入允许编号中的英文句点，保留原始编号和卡密；密码仅保存哈希。
- 原始卡密表不得放入Git仓库、发布包或操作日志。上线代码不等于完成导入；必须另外通过已认证后台导入，并核对后台卡券数量。
